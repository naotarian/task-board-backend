# === 🔐 Django secret key（↓初回だけ生成してもOK） ===
SECRET_KEY=your-dev-secret-key-here
# 生成コマンド(Dockerコンテナ内)
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# === 🛢️ Database settings ===
DB_NAME=app_db
DB_USER=user
DB_PASSWORD=password
DB_HOST=db
DB_PORT=3306
